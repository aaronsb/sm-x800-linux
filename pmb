#!/bin/bash
# Repo-local pmbootstrap wrapper: always the vendored checkout, regardless of
# where the repo lives or what's on PATH.
exec python3 "$(dirname "$(readlink -f "$0")")/tools/pmbootstrap/pmbootstrap.py" "$@"
